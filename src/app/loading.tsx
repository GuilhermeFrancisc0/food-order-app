import React from 'react'

const Loading = () => (
  <div className="flex items-center justify-center min-h-[100vh]">
    <span className="loading loading-infinity bg-primary w-20"></span>
  </div>
)

export default Loading;